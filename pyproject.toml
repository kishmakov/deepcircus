[project]
name = "deepcircus"
version = "0.1.0"
description = "Learn decision-tree depth and size from sampled Boolean functions"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "pyyaml>=6.0.3",
    "torch>=2.12.0",
    "tqdm>=4.68.2",
]
