# cpp/common/tools

Generator-independent utilities in namespace `tools`:

- exact decision-tree solvers for the models used by the project;
- deterministic random-number utilities shared by generation and preparation;
- a binary tree with a `uint32_t` payload per node.
