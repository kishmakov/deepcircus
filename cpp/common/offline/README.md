# cpp/common/offline

Reader and writer for packed offline training data. The format itself is
specified in [`docs/data_m1.md`](../../../docs/data_m1.md).

[`../../preparation/`](../../preparation/README.md) produces these files, and
the test suite covers their round trip.
